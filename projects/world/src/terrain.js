const SIZE=96,STEP=3,HALF=(SIZE-1)*STEP*.5;export class Terrain{constructor(gl,seed=43129){this.gl=gl;this.seed=seed;this.vbo=gl.createBuffer();this.ibo=gl.createBuffer();this.count=0;this.build(0,0)}noise(x,z){const n=Math.sin((x+this.seed)*.013)*43758.5453+Math.sin((z-this.seed)*.017)*19623.3421;return(n-Math.floor(n))*2-1}height(x,z){return this.noise(x*.7,z*.7)*3+this.noise(x*.19,z*.19)*10+this.noise(x*.045,z*.045)*24+Math.sin(x*.025)*5}build(px,pz){const v=[],i=[];for(let z=0;z<SIZE;z++)for(let x=0;x<SIZE;x++){const wx=Math.floor(px/HALF)*HALF+(x*STEP-HALF),wz=Math.floor(pz/HALF)*HALF+(z*STEP-HALF);v.push(wx,this.height(wx,wz),wz)}for(let z=0;z<SIZE-1;z++)for(let x=0;x<SIZE-1;x++){const n=z*SIZE+x;i.push(n,n+1,n+SIZE,n+1,n+SIZE+1,n+SIZE)}const gl=this.gl;gl.bindBuffer(gl.ARRAY_BUFFER,this.vbo);gl.bufferData(gl.ARRAY_BUFFER,new Float32Array(v),gl.DYNAMIC_DRAW);gl.bindBuffer(gl.ELEMENT_ARRAY_BUFFER,this.ibo);gl.bufferData(gl.ELEMENT_ARRAY_BUFFER,new Uint32Array(i),gl.STATIC_DRAW);this.count=i.length}}